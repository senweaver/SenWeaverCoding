[package]
name = "sen-tui"
version = "0.1.0"
edition = "2024"

[dependencies]
senweavercoding = { path = "../..", package = "SenWeaverCoding", default-features = false, features = ["tui"] }

[features]
default = ["tui"]
tui = ["senweavercoding/tui"]
